language: ruby
rvm:
  - 2.3.0

services:
  - postgresql

before_script:
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database cats_travis_test;' -U postgres
  - "rake db:migrate RAILS_ENV=test"
