<div class="wrapper wrapper-content">
  <div class="ibox float-e-margins">
    <div class="ibox-content">
      <%= form_for(project_code_allocation) do |f| %>
        <% if project_code_allocation.errors.any? %>
          <div class="panel panel-danger">
            <div class="panel-heading">
              <%= pluralize(project_code_allocation.errors.count, "error") %> prohibited this project_code_allocation from being saved.
            </div>

            <div class="panel-body">
              <ul>
                <% project_code_allocation.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        <% end %>

                <div class="form-group">
                            <%= f.label :hub_id %>
                <%= f.number_field :hub_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :warehouse_id %>
                <%= f.number_field :warehouse_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :store_id %>
                <%= f.number_field :store_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :operation_id %>
                <%= f.number_field :operation_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :requisition_id %>
                <%= f.number_field :requisition_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :amount %>
                <%= f.text_field :amount, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :unit_of_measure_id %>
                <%= f.number_field :unit_of_measure_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :fdp_id %>
                <%= f.number_field :fdp_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :project_id %>
                <%= f.number_field :project_id, :class => 'form-control' %>
                        </div>

      
      <div class="actions">
        <%= f.submit :class => 'btn btn-primary' %>
      </div>
  <% end %>

    </div>
  </div>
</div>