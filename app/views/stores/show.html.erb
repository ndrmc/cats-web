    <p>
      <strong>Name:</strong>
      <%= @store.name %>
    </p>

    <p>
      <strong>Temporary:</strong>
      <%= @store.temporary %>
    </p>

    <p>
      <strong>Hub:</strong>
      <%= @store.hub_id %>
    </p>

    <p>
      <strong>Store owner:</strong>
      <%= @store.store_owner_id %>
    </p>

    <p>
      <strong>Store location:</strong>
      <%= @store.warehouse_id %>
    </p>

<%= link_to 'Edit', edit_store_path(@store) %> |
<%= link_to 'Back', warehouse_stores_path(@store.warehouse_id,@store) %>
