<div class="wrapper wrapper-content">
  <div class="ibox float-e-margins">
    <div class="ibox-content">
      <%= form_for(requisition) do |f| %>
        <% if requisition.errors.any? %>
          <div class="panel panel-danger">
            <div class="panel-heading">
              <%= pluralize(requisition.errors.count, "error") %> prohibited this requisition from being saved.
            </div>

            <div class="panel-body">
              <ul>
                <% requisition.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        <% end %>

                <div class="form-group">
                            <%= f.label :requisition_no %>
                <%= f.text_field :requisition_no, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :operation_id %>
                <%= f.number_field :operation_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :commodity_id %>
                <%= f.text_field :commodity_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :region_id %>
                <%= f.text_field :region_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :zone_id %>
                <%= f.text_field :zone_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :ration_id %>
                <%= f.text_field :ration_id, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :requested_by %>
                <%= f.text_field :requested_by, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :requested_on %>
                <%= f.date_select :requested_on, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :status %>
                <%= f.number_field :status, :class => 'form-control' %>
                        </div>

      
      <div class="actions">
        <%= f.submit :class => 'btn btn-primary' %>
      </div>
  <% end %>

    </div>
  </div>
</div>