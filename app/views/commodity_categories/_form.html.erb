<div class="wrapper wrapper-content">
  <div class="ibox float-e-margins">
    <div class="ibox-content">
      <%= form_for(commodity_category) do |f| %>
        <% if commodity_category.errors.any? %>
          <div class="panel panel-danger">
            <div class="panel-heading">
              <%= pluralize(commodity_category.errors.count, "error") %> prohibited this commodity_category from being saved.
            </div>

            <div class="panel-body">
              <ul>
                <% commodity_category.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          </div>
        <% end %>

                <div class="form-group">
                            <%= f.label :name %>
                <%= f.text_field :name, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :code %>
                <%= f.text_field :code, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :code_am %>
                <%= f.text_field :code_am, :class => 'form-control' %>
                        </div>

                <div class="form-group">
                            <%= f.label :description %>
                <%= f.text_field :description, :class => 'form-control' %>
                        </div>

      
      <div class="actions">
        <%= f.submit :class => 'btn btn-primary' %>
      </div>
  <% end %>

    </div>
  </div>
</div>